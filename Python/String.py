String Manipulation:

    len(string): Returns the length of the string.
    string.strip(): Removes leading and trailing whitespace from the string.
    string.lstrip(): Removes leading whitespace from the string.
    string.rstrip(): Removes trailing whitespace from the string.
    string.upper(): Converts the string to uppercase.
    string.lower(): Converts the string to lowercase.
    string.replace(old, new): Replaces all occurrences of old with new in the string.
    string.split(separator): Splits the string into a list of substrings based on the separator.
    string.join(iterable): Concatenates elements of an iterable into a single string, using string as the separator.
    string.format(args): Formats the string by replacing placeholders with specified args.
    string.isalpha(), string.isdigit(), string.isalnum(), string.isspace(): Checks if the string consists of alphabetic characters, digits, alphanumeric characters, or whitespace, respectively.

String Searching and Comparison:

    string.find(substring): Returns the index of the first occurrence of substring in the string.
    string.rfind(substring): Returns the index of the last occurrence of substring in the string.
    string.startswith(prefix), string.endswith(suffix): Checks if the string starts with prefix or ends with suffix.
    string.count(substring): Returns the number of non-overlapping occurrences of substring in the string.
    string.index(substring): Returns the index of the first occurrence of substring in the string.
    string.rindex(substring): Returns the index of the last occurrence of substring in the string.
    string == other_string, string != other_string, string < other_string, string > other_string: Comparison operators to compare strings.

String Conversion:

    str(value): Converts a value to a string representation.
    int(string), float(string): Converts a string to an integer or float.
    ord(char): Returns the Unicode code point of the character.
    chr(code): Returns the character represented by the Unicode code point.

String Validation and Checking:

    string.isalpha(), string.isdigit(), string.isalnum(), string.isspace(): Checks if the string consists of alphabetic characters, digits, alphanumeric characters, or whitespace, respectively.
    string.islower(), string.isupper(): Checks if the string contains lowercase or uppercase letters.
